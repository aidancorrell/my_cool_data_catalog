import * as React from "react";
import type { IEvent } from "../models/event";
import { GraphModel } from "../models/GraphModel";
import type { IGraphState } from "../models/state";
export declare const defaultGraphStateContext: IGraphStateContext;
export declare const EMPTY_CONNECT_STATE: {
    sourceNode: undefined;
    sourcePort: undefined;
    targetNode: undefined;
    targetPort: undefined;
    movingPoint: {
        x: number;
        y: number;
    };
};
export type IGraphAction<NodeData = unknown, EdgeData = unknown, PortData = unknown> = IEvent<NodeData, EdgeData, PortData>;
export type IDispatchCallback<NodeData = unknown, EdgeData = unknown, PortData = unknown> = (state: IGraphState<NodeData, EdgeData, PortData>, prevState: IGraphState<NodeData, EdgeData, PortData>) => void;
export type IDispatch<NodeData = unknown, EdgeData = unknown, PortData = unknown, Action = never> = (action: IEvent<NodeData, EdgeData, PortData> | Action, callback?: IDispatchCallback<NodeData, EdgeData, PortData>) => void;
export interface IGraphStateContext<NodeData = unknown, EdgeData = unknown, PortData = unknown, Action = never> {
    state: IGraphState<NodeData, EdgeData, PortData>;
    dispatch: IDispatch<NodeData, EdgeData, PortData, Action>;
}
/**
 * use separate context for now to improve performance
 * until https://github.com/reactjs/rfcs/pull/119 or something equivalent
 */
export declare const GraphValueContext: React.Context<GraphModel<unknown, unknown, unknown>>;
export declare const GraphStateContext: React.Context<IGraphStateContext<unknown, unknown, unknown, never>>;
export type IGraphReactReducer<NodeData = unknown, EdgeData = unknown, PortData = unknown, Action = never> = React.Reducer<IGraphState<NodeData, EdgeData, PortData>, IEvent<NodeData, EdgeData, PortData> | Action>;
export type IGraphReducer<NodeData = unknown, EdgeData = unknown, PortData = unknown, Action = never> = (next: IGraphReactReducer<NodeData, EdgeData, PortData, Action>) => IGraphReactReducer<NodeData, EdgeData, PortData, Action>;
export declare const setData: <NodeData = unknown, EdgeData = unknown, PortData = unknown>(state: IGraphState<NodeData, EdgeData, PortData>, data: GraphModel<NodeData, EdgeData, PortData>) => IGraphState<NodeData, EdgeData, PortData>;
export declare const updateData: <NodeData = unknown, EdgeData = unknown, PortData = unknown>(state: IGraphState<NodeData, EdgeData, PortData>, f: (data: GraphModel<NodeData, EdgeData, PortData>) => GraphModel<NodeData, EdgeData, PortData>) => IGraphState<NodeData, EdgeData, PortData>;
