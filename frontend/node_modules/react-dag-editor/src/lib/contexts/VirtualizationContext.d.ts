/// <reference types="react" />
import { IRectShape, IViewport } from "../models/geometry";
export interface IVirtualizationContext {
    viewport: Required<IViewport>;
    visibleArea: IRectShape;
    renderedArea: IRectShape;
    renderedNodes: Set<string>;
    renderedEdges: Set<string>;
    timestamp: number;
}
export declare const VirtualizationContext: import("react").Context<IVirtualizationContext>;
