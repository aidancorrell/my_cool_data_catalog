import { ICanvasData } from "../../../index";
import { Task } from "./cpm";
export interface IDuration {
    nodeId: string;
    duration: number;
}
export declare const getCriticalPath: (canvasData: ICanvasData, durations: Map<string, number>, isNodesSorted?: boolean) => Map<string, {
    task: Task;
    isCritical?: boolean;
}>;
export declare const markCriticalPath: (path: Task, ret: Map<string, {
    task: Task;
    isCritical?: boolean | undefined;
}>, isCritical?: boolean, parentTask?: Task) => void;
