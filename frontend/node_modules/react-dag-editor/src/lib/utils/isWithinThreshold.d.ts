import { IPoint } from "../models/geometry";
export declare const isWithinThreshold: (dx: number, dy: number, threshold: number) => boolean;
export declare const isWithinRect: (p0: IPoint, p: IPoint, w: number, h: number) => boolean;
