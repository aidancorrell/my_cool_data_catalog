import * as React from "react";
import { IContainerRect } from "../models/geometry";
import { EventChannel } from "../utils/eventChannel";
import { ITouchHandler } from "./TouchController";
/**
 * debug helpers, just leave them here
 */
export declare class TwoFingerHandler implements ITouchHandler {
    private readonly rectRef;
    private readonly eventChannel;
    private prevEvents;
    private prevDistance;
    constructor(rectRef: React.RefObject<IContainerRect | undefined>, eventChannel: EventChannel);
    onEnd(): void;
    onMove(pointers: Map<number, PointerEvent>, e: PointerEvent): void;
    onStart(pointers: Map<number, PointerEvent>): void;
}
