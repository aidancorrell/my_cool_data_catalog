import * as React from "react";
import { IViewport } from "../models/geometry";
import { EventChannel } from "../utils/eventChannel";
export interface IVirtualizationProviderProps {
    viewport: Required<IViewport>;
    isVirtualizationEnabled: boolean;
    virtualizationDelay: number;
    eventChannel: EventChannel;
}
export declare const VirtualizationProvider: React.FunctionComponent<React.PropsWithChildren<IVirtualizationProviderProps>>;
