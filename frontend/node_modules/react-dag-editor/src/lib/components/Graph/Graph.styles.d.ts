import { IGraphState } from "../../models/state";
import { IGraphProps } from "./IGraphProps";
interface IGraphStyles {
    svg: string;
    container: string;
    buttonA11y: string;
    node: string;
}
export declare const getGraphStyles: (props: IGraphProps, state: IGraphState, isPanDisabled: boolean, isNodeDraggable: boolean, focusedWithoutMouse: boolean, isMultiSelecting: boolean) => IGraphStyles;
export {};
