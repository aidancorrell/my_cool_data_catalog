export interface IDeferredValueConfig {
    timeout: number;
}
/**
 * @todo migrate to React.useDeferredValue after Concurrent mode release
 */
export declare function useDeferredValue<T>(value: T, { timeout }: IDeferredValueConfig): T;
