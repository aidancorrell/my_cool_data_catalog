import { IEventProvider, IGlobalMoveEventTypes } from "./types";
export declare class PointerEventProvider implements IEventProvider<IGlobalMoveEventTypes> {
    private readonly target;
    private readonly pointerId;
    private readonly eventEmitter;
    constructor(target: Window | HTMLElement, pointerId?: number | null);
    off<Type extends keyof IGlobalMoveEventTypes>(type: Type, callback: (...args: IGlobalMoveEventTypes[Type]) => void): PointerEventProvider;
    on<Type extends keyof IGlobalMoveEventTypes>(type: Type, callback: (...args: IGlobalMoveEventTypes[Type]) => void): PointerEventProvider;
    private ensureAddListener;
    private ensureRemoveListener;
    private readonly onMove;
    private readonly onUp;
}
